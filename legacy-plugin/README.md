# Legacy Plugin Snapshot

This folder contains the previous plugin-oriented implementation kept only for history/reference.

Do not deploy this folder as the main solution.
Use the standalone workflow from the project root (`index.php`).
